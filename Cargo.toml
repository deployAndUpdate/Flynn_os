[workspace]
resolver = "2"
members = ["kernel"]

[package]
name = "flynn_os"
version = "0.1.0"
edition = "2021"

[build-dependencies]
kernel = { path = "kernel", artifact = "bin", target = "x86_64-unknown-none" }
bootloader = "0.11"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
